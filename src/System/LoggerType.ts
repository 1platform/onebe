/**
 * A list with supported loggers from the application.
 *
 * @enum
 */
enum LoggerType {
  CONSOLE = "console",
  FILE = "file",
  JSON = "json",
  JSON_FILE = "json_file",
  NO_LOGGER = "no_logger",
}

export default LoggerType;
